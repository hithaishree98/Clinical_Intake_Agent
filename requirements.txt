fastapi>=0.110,<1.0
uvicorn>=0.24,<1.0
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0

# LangGraph + checkpoint store: pin compatible upper bounds.
# Without these caps, pip resolves langgraph-checkpoint-sqlite to a 3.x
# release (which imports `get_checkpoint_metadata` from langgraph.checkpoint.base)
# while leaving langgraph + langgraph-checkpoint at 0.2.x / 1.0.x (where that
# symbol doesn't exist).  Result: ImportError at app import time.
# See https://github.com/langchain-ai/langgraph/issues for ecosystem details.
langgraph>=0.2,<0.3
langgraph-checkpoint>=1.0,<2.0
langgraph-checkpoint-sqlite>=1.0,<2.0

google-genai>=0.3,<2.0
python-multipart>=0.0.5,<1.0
slowapi>=0.1.9,<1.0
PyJWT>=2.8,<3.0
# Voice transcription (optional). The /transcribe endpoint short-circuits to
# 503 when GROQ_API_KEY is unset, so the dependency only matters when voice
# is enabled at runtime.
groq>=0.4,<1.0

# Dev / CI tooling.  Pinned loosely so security audits pick up new fixes.
pytest>=8.0
bandit>=1.7
pip-audit>=2.7
